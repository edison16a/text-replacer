{
  "//": "Type checking only. Nothing is compiled or emitted: the extension ships the source in this repo exactly as written. This makes the JSDoc comments load-bearing instead of decorative, and gives us chrome's own API types so a wrong argument is an error rather than a runtime surprise in a browser.",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "strict": true,
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable", "WebWorker"],
    "types": ["chrome", "node"],
    "skipLibCheck": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitOverride": true,
    "exactOptionalPropertyTypes": false
  },
  "include": ["src/**/*.js", "tools/**/*.mjs"]
}
